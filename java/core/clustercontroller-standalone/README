# Start a cluster controller, using only web socket transport (local communication only)

## via Maven:
mvn exec:java -Dexec.mainClass="io.joynr.runtime.ClusterController" -Dexec.args="-t websocketcc"

## from the JAR in target directory:
java -jar clustercontroller-standalone-1.15.1-jar-with-dependencies.jar -t websocketcc


# Start a cluster controller, connecting to a local MQTT broker

## via Maven:
mvn exec:java -Dexec.mainClass="io.joynr.runtime.ClusterController" -Dexec.args="-t mqtt"

## from the JAR in target directory:
java -jar clustercontroller-standalone-1.15.1-jar-with-dependencies.jar -t mqtt


## Show cluster controller usage info for additional options

## via Maven:
mvn exec:java -Dexec.mainClass="io.joynr.runtime.ClusterController" -Dexec.args="-h"

## from the JAR in target directory:
java -jar clustercontroller-standalone-1.15.1-jar-with-dependencies.jar -h

